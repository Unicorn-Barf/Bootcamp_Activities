// TODO: What will the following line of code log?
// Global window object
console.log(window);

// To console length of the window object
let myObj = Object.keys(window).length;
console.log(myObj);

// TODO: What will the following line of code log?
// the document object that is a property of the windown object
console.log(window.document);

// TODO: What will the following line of code log?
// HTML elements
console.log(document.documentElement);

// TODO: What will the following line of code log?
// the head of the html
console.log(document.head);

